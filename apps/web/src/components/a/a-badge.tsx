import * as React from "react";
import { Badge, type BadgeProps } from "@/components/ui/badge";

/** AUTHORITY status badge — tokens only. */
export function ABadge(props: BadgeProps) {
  return <Badge {...props} />;
}
