import SaStubPage from "@/components/super-admin/sa-stub-page";

export default function Page() {
  return (
    <SaStubPage
      title="Modules"
      blurb="Activation société — API existante, UI chrome."
    />
  );
}
