import SaStubPage from "@/components/super-admin/sa-stub-page";

export default function Page() {
  return (
    <SaStubPage
      title="Jobs / DLQ"
      blurb="Files Thunder (dont print) — shell CC, pas AppShell métier."
    />
  );
}
