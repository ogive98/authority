---
description: AUTHORITY D294 visual constitution — Graphic Master Pack v2 supersedes Soft Glass and ZIP multi-accent
alwaysApply: true
---

# Visual identity lock — D294 AUTHORITY Premium

**Decision 2026-09-17 (product):** `GRAPHIC_MASTER_PACK_v2` / D294 is the
**visual source of truth**. Soft Glass D161 and ZIP multi-accent (violet/sky/orange
chrome) are **superseded**.

Docs: `04_UI_UX/GRAPHIC_MASTER_PACK_v2/00_START_HERE.md` · `10_TOKENS.md` ·
`40_CURSOR_MASTER.md`

**Still locked (non-visual):** registry-driven nav · no fake KPI · Prefs VALIDATED ·
features never in sidebar · Smart Action Dock = right rail · backend unchanged.

## Before any UI work

1. Read `apps/web/src/app/globals.css` (`--a-*` D294 only).
2. Reuse `AppShell` + Mission Control + Smart Action Dock + `A*`.
3. One accent only (`--a-accent`). No module colors.
4. Registry-driven — never hardcode sidebar modules/actions.

## Locked identity (D294)

| Token | Light | Dark |
|-------|--------|------|
| Canvas | `#f5f6f8` | `#14161a` |
| Surface 2 | `#ffffff` | `#1b1e24` |
| Accent | `#2f5fdd` | `#6f93e8` |
| Font | system sans + mono numérique | same |
| Radius | 6 / 10 / 14px | same |
| Primary CTA | one visible primary per screen | same |

## Forbidden (D294)

- Soft Glass materials as SoT (blur page surfaces, teal Soft Glass identity)
- Multi-accent chrome (`--a-violet` / `--a-sky` / `--a-orange` as design accents)
- Decorative gradients (except login if explicitly allowed)
- Page-surface blur / glass vibrancy
- Second parallel design system
- Hardcoded fake dashboard metrics
- Features inside the sidebar
