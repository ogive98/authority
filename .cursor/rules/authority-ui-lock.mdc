---
description: AUTHORITY visual identity — Progressive Intelligence OS (ZIP pack v1) supersedes Soft Glass D161
alwaysApply: true
---

# Visual identity lock — Progressive Intelligence OS (ZIP pack v1)

**Decision 2026-09-16 (product):** the Cursor pack
`AUTHORITY_UIUX_PROGRESSIVE_REFACTOR_CURSOR_PACK_v1` + `12_SCREENSHOTS/` is the
**visual source of truth**. Soft Glass Enterprise OS **D161 is superseded** for
colors, chrome, cards, and action grammar.

**Still locked (non-visual):** registry-driven nav · no fake KPI/CA inventés ·
Prefs VALIDATED rates · features never in sidebar · Smart Action Dock = right rail ·
backend/API/business rules unchanged.

**Supersedes:** Soft Glass D161 · Contiental Apple · Utility Cube · Wick.

Reference pixels: `04_UI_UX/PROGRESSIVE_REFACTOR_PACK_v1/12_SCREENSHOTS/`.

## Before any UI work

1. Read `apps/web/src/app/globals.css` (`--a-*` only — ZIP-tuned).
2. Reuse `AppShell` + Mission Control + Smart Action Dock + `A*`.
3. Match references: `/` · `/dev/primitives` · list/detail anatomy from pack.
4. Registry-driven — never hardcode sidebar modules/actions/KPI inventions.

## Locked identity (ZIP)

| Token | Value |
|-------|--------|
| Fonts | Outfit + JetBrains Mono (tabular) |
| Canvas | Near-black / charcoal `#0b0e14` (dark default) |
| Surfaces | Raised panels `#12161f`–`#181e2a` with **subtle hairline** |
| Accent primary | Electric blue / cyan `#4f8cff` · `#22d3ee` |
| Accents (actions) | Blue · cyan · violet · emerald · orange (semantic diamonds) |
| Radius | Soft panels ~8–12px (not Soft Glass 2px tight-only) |
| Cards | Allowed as ZIP panels (subtle border + fill) — not Bootstrap boxes |
| Primary CTA | Luminous accent text / light fill (e.g. « + New order ») — not heavy teal frame |
| Density | ERP-first, scannable |

## Chrome (mandatory — ZIP anatomy)

```text
┌ topbar: AUTHORITY wordmark · ⌘K search · FR · Online · clock · (ops/user) ─┐
├ sidebar modules (registry) ┬ main (Mission Control / list / detail) ┬ SMART ACTIONS ┤
└────────────────────────────┴────────────────────────────────────────┴───────────────┘
```

- Features **never** in sidebar → Mission Control / module feature list
- Right rail = Smart Action Dock only (cyan « SMART ACTIONS » title)
- Ops modes exit only via calculator unlock
- Live KPIs only (`/home-kpis`) — layout may match ZIP cards; **values never invented**

## Forbidden

- Reverting to Soft Glass zero-frame / teal-only identity as SoT
- Second parallel design system beside this ZIP OS
- Hardcoded fake dashboard metrics (€ / CA)
- Feature lists inside the sidebar
- Replacing Smart Action Dock with a module ContextRail
